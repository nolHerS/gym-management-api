# application.yml - common properties
# This file is intentionally minimal. Use profiles for environment-specific settings:
# - application-dev.yml : enables Swagger UI and sets a local dev user (admin/adminpass).
# - application-prod.yml: disables Swagger endpoints.
# Run in dev:  mvn spring-boot:run -Dspring-boot.run.profiles=dev
# Run in prod: mvn spring-boot:run -Dspring-boot.run.profiles=prod

# Add common properties below as needed.
